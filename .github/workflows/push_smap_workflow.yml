# Copyright 2025 Lincoln Institute of Land Policy
# SPDX-License-Identifier: Apache-2.0

name: Push SMAP Workflow to GHCR

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group:
    ${{ github.workflow }}-${{ github.ref_name }}-${{
    github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  build_and_push:
    name: "build and push smap workflow image"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and export
        uses: docker/build-push-action@v6
        with:
          context: SMAP_ETL
          file: SMAP_ETL/Dockerfile
          push: true
          tags: ghcr.io/cgs-earth/asu-awo-smap-etl:latest
          cache-from: type=gha,scope=asu-awo-smap-etl
          platforms: linux/amd64,linux/arm64
          cache-to: type=gha,mode=max,scope=asu-awo-smap-etl
